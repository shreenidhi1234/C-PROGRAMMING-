#include<stdio.h>

#include<conio.h>

int main()

{

	int num,i,fact = 1;	
  printf("\nenter the number :");

	scanf("%d",&num);

	for(i=1;i<=num;i++)

	{

		fact=fact*i;

	    printf("\n %d",fact);

	}

	getch();

	return 0;

	}
